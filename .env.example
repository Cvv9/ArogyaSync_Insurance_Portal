# Insurance Verification API base URL (no trailing slash)
# Local dev: http://localhost:10000
# Production: https://api.arogyasync.com/csv
VITE_API_BASE_URL=http://localhost:10000

# API key — must match the API_KEY in the Insurance Verification API's .env
VITE_API_KEY=replace_with_matching_api_key
